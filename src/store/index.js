import { createStore } from 'vuex'

export default createStore({
  state: {
    // 初期状態を定義
  },
  mutations: {
    // 状態を変更するメソッドを定義
  },
  actions: {
    // 非同期処理を含むメソッドを定義
  },
  modules: {
    // 複数のストアをモジュールとして管理
  }
})